class Question{

    String questionText;
    bool questionAnswer;

    Question(this.questionText, this.questionAnswer);
}//Question class with constructor which set the two property when we call it.